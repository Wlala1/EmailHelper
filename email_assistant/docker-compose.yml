services:
  email_assistant:
    images: email_assistant:latest
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    networks:
      - n8n-network

networks:
  n8n-network:
    external: true
